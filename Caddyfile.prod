{$APP_DOMAIN} {
	# REST API
	handle_path /api/* {
		reverse_proxy api:4000
	}

	# Socket.IO (WebSocket upgrade)
	handle /socket.io/* {
		reverse_proxy api:4000
	}

	# PWA frontend (SPA)
	handle {
		reverse_proxy web:80
	}
}
